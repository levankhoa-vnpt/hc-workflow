Bạn đang ở chế độ PHÂN TÍCH — TUYỆT ĐỐI không tạo, không sửa, không xóa bất kỳ file nào, không tạo branch, không commit. Chỉ đọc và suy nghĩ.

Trước khi phân tích/code, đọc NAMING_CONVENTIONS.md và áp dụng nghiêm ngặt cho mọi thành phần MỚI được tạo ra; giữ nguyên convention cũ khi sửa code đã tồn tại.

1. Đọc AGENTS.md và bảng định tuyến để xác định yêu cầu dưới đây liên quan tới (những) repo nào.
2. Đọc trực tiếp code hiện có liên quan trong (các) repo đó (entity, service, controller, component... tùy domain) để hiểu logic nghiệp vụ hiện tại, tránh đề xuất trùng lặp hoặc phá vỡ convention đang dùng.
2b. Nếu yêu cầu liên quan tới giao diện/luồng nghiệp vụ đang chạy (không phải tính năng hoàn toàn mới chưa tồn tại), dùng chrome-devtools MCP để mở trực tiếp môi trường test và quan sát thực tế: đọc thông tin đăng nhập từ .opencode/test-env.local.md (nếu không có, đọc .opencode/test-env.md để lấy URL rồi hỏi người dùng credential), đăng nhập, điều hướng tới màn hình/luồng liên quan, chụp snapshot DOM để hiểu UI/UX và hành vi hiện tại. Đây là bước CHỈ XEM, không thao tác thay đổi dữ liệu thật.
3. Sau khi đọc xong (bước 1, 2, 2b), tự đánh giá: yêu cầu gốc của tôi đã đủ CỤ THỂ và ĐO LƯỜNG ĐƯỢC để giao thẳng cho /work code hay chưa? Coi là CHƯA ĐỦ nếu rơi vào bất kỳ trường hợp nào sau: (a) có nhiều hơn 1 cách hiểu hợp lý về phạm vi công việc, (b) không rõ đây là fix bug / thêm tính năng / sửa UI hay toàn bộ, (c) thiếu tiêu chí xác nhận hoàn thành đo lường được, (d) có quyết định nghiệp vụ mà chỉ tôi (người dùng) mới trả lời được.

4. RẼ NHÁNH BẮT BUỘC — CHỈ ĐƯỢC CHỌN MỘT TRONG HAI, KHÔNG ĐƯỢC LÀM CẢ HAI:

   NHÁNH A — Nếu CHƯA ĐỦ thông tin (theo bước 3): CHỈ xuất ra danh sách câu hỏi cần tôi trả lời, dưới dạng:
   ---
   SPEC CHƯA ĐỦ THÔNG TIN. Cần bạn trả lời trước khi tạo lệnh /work:
   1. <câu hỏi>
   2. <câu hỏi>
   ...
   ---
   TUYỆT ĐỐI KHÔNG xuất khối /work ở nhánh này, kể cả dưới dạng nháp/tạm/ví dụ. Dừng lại, chờ tôi trả lời.

   NHÁNH B — Nếu ĐÃ ĐỦ thông tin: xuất đúng format sau, KHÔNG kèm câu hỏi nào cả (nếu còn câu hỏi tức là chưa đủ, phải quay về NHÁNH A):
   ---
   REPO CẦN CHẠM: <liệt kê repo, lý do chọn>
   PHẠM VI THAY ĐỔI: <các file/module/class dự kiến động tới, dựa trên code đã đọc>
   RỦI RO / ĐIỂM CẦN LƯU Ý: <xung đột với logic cũ, ảnh hưởng module khác, breaking change nếu có>

   /work <mô tả yêu cầu đầy đủ, đã làm rõ, viết lại rõ ràng dựa trên phân tích trên, PHẢI là yêu cầu CODE thật sự, không phải yêu cầu "chỉ phân tích" hay "chưa code">
   Kết quả mong muốn: <acceptance criteria cụ thể, đo lường được, dựa trên hiểu biết về code hiện tại>
   ---

Yêu cầu cần phân tích:
{{args}}